guard :jammit, :config_path => '_assets_javascripts.yml', :output_folder => 'javascripts/'  do
  watch(%r{^javascripts/(.*)\.js$})
end

guard :jammit, :config_path => '_assets_stylesheets.yml', :output_folder => 'stylesheets/'  do
  watch(%r{^stylesheets/(.*)\.css$})
end